function everyOtherCap (string) {
  let finalString = ""
  for(i=0; i<=string.length; i++) {
    if (i%2 === 0)
      finalString += string.charAt(i)
    else
      finalString += string.charAt(i).toUpperCase()
  }
  return finalString
}
